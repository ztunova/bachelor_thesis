# contour
# hierarchy
# type rectangle, ellipse, triangle, diamond

class Shape:
    def __init__(self, contour, hierarchy, shape_name):
        self.contour = contour
        self.hierarchy = hierarchy
        self.shape_name = shape_name
